---
title: YouTube
icon: Youtube
---
## `Permission denied (EACCES)` error in the console when running the `/yt` command

Make sure `telegram/plugins/yt-dlp/yt-dlp` is executable! You can do this on Linux like so:

```bash
chmod +x telegram/plugins/yt-dlp/yt-dlp
```